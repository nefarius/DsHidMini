<Page
    x:Class="Nefarius.DsHidMini.ControlApp.Views.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Nefarius.DsHidMini.ControlApp.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="SettingsPage"
    d:DataContext="{d:DesignInstance local:SettingsPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ScrollViewer.CanContentScroll="False"
    mc:Ignorable="d">
    <ScrollViewer
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <CheckBox
                Margin="0,0,0,8"
                Content="Automatically restart devices on HID mode mismatch"
                IsChecked="{Binding ViewModel.AutoRestartOnHidModeMismatch, Mode=TwoWay}" />
            <TextBlock
                MaxWidth="600"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="When enabled, DsHidMini requests a device restart on its own if the HID mode it loaded from configuration doesn't match the mode already probed by Windows, instead of requiring a manual reconnect or a second replug."
                TextWrapping="Wrap" />
            <CheckBox
                Margin="0,16,0,8"
                Content="Minimize to tray"
                IsChecked="{Binding ViewModel.MinimizeToTray, Mode=TwoWay}" />
            <TextBlock
                MaxWidth="600"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="When enabled, Minimize and Close hide ControlApp to the notification area. Open the window from the tray icon, or choose Exit to quit."
                TextWrapping="Wrap" />
            <CheckBox
                Margin="0,16,0,8"
                Content="Automatically switch Retro Fighters Defender BT to PS3 mode"
                IsChecked="{Binding ViewModel.AutoSwitchDefenderBtToPs3Mode, Mode=TwoWay}" />
            <TextBlock
                MaxWidth="600"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="When enabled, a Retro Fighters Defender Bluetooth Edition detected in DualShock 4 mode is switched into PS3 (DualShock 3) mode automatically, instead of requiring the 'Switch to PS3 mode' button on the Devices page."
                TextWrapping="Wrap" />

            <CheckBox
                Margin="0,16,0,8"
                Content="Enable driver IPC"
                IsChecked="{Binding ViewModel.IPCEnabled, Mode=TwoWay}" />
            <TextBlock
                MaxWidth="600"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="When enabled (default), DsHidMini exposes shared-memory IPC for pairing, power-off, and similar ControlApp commands. Changes apply immediately without reloading the driver."
                TextWrapping="Wrap" />

            <ui:TextBlock
                Margin="0,28,0,8"
                FontTypography="Subtitle"
                Text="Bluetooth (BthPS3)" />
            <ui:InfoBar
                Title="{Binding ViewModel.BthPs3.StatusTitle}"
                IsClosable="False"
                IsOpen="True"
                Message="{Binding ViewModel.BthPs3.StatusMessage}"
                Severity="{Binding ViewModel.BthPs3.Severity}" />
            <TextBlock
                Margin="0,12,0,4"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="{Binding ViewModel.BthPs3.InstalledVersionDisplay, StringFormat=Installed version: {0}}" />
            <TextBlock
                Margin="0,0,0,4"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="{Binding ViewModel.BthPs3.RequiredVersionDisplay, StringFormat=Required version: {0}+}" />
            <TextBlock
                Margin="0,0,0,4"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="{Binding ViewModel.BthPs3.PsmPatchingDisplay, StringFormat=PSM patching: {0}}" />
            <TextBlock
                Margin="0,0,0,12"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="{Binding ViewModel.BthPs3.RawPdoDisplay, StringFormat=RAW PDO: {0}}" />
            <StackPanel Margin="0,0,0,8" Orientation="Horizontal">
                <ui:Button
                    Margin="0,0,8,0"
                    Command="{Binding ViewModel.RectifyBthPs3SettingsCommand}"
                    Content="Fix settings"
                    IsEnabled="{Binding ViewModel.BthPs3.CanRectifySettings}" />
                <ui:Button Command="{Binding ViewModel.RefreshBthPs3Command}" Content="Refresh" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <ui:HyperlinkButton
                    Margin="0,0,8,0"
                    Content="Download BthPS3"
                    Icon="{ui:SymbolIcon ArrowDownload24}"
                    NavigateUri="https://github.com/nefarius/BthPS3/releases/latest" />
                <ui:HyperlinkButton
                    Content="Open online help"
                    Icon="{ui:SymbolIcon QuestionCircle24}"
                    NavigateUri="https://docs.nefarius.at/projects/BthPS3/" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
